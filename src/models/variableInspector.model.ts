export type VariableInspectorRowModel = {
    index:number,
    isScrolling:boolean,
    isVisible:boolean,
    key:string,
    parent:any,
    style:any
}

export type VariableInspectorCellModel = {
    columnIndex:number,
    isScrolling:boolean,
    isVisible:boolean,
    key:string,
    parent:any,
    rowIndex:number,
    style:any
}